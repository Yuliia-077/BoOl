@page
@model BoOl.Pages.Types.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h4 class="text-center">Ви дійсно хочете видалити дану модель?</h4>
<hr />
<div class="row">
    <div class="col-1">
        <a asp-page="./Details" asp-route-id="@Model.Model.Id"><i class="fas fa-arrow-left"></i></a>
    </div>
    <div class="col-11">
        <dl class="row">
            <dt class="col-6 text-right">
                Виробник:
            </dt>
            <dd class="col-6">
                @Html.DisplayFor(model => model.Model.Manufacturer)
            </dd>
            <dt class="col-6 text-right">
                Модель:
            </dt>
            <dd class="col-6">
                @Html.DisplayFor(model => model.Model.Type)
            </dd>
        </dl>
        <form method="post" class="text-center">
            <input type="hidden" asp-for="Model.Id" />
            <input type="submit" value="Видалити" class="btn btn-danger" />
        </form>
    </div>
</div>
